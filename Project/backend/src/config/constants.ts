export const API_MODE = `${process.env.API_MODE}`;
export const DB_URL = `${process.env.DB_URL}`;

export const JWT_SECRET = `${process.env.JWT_SECRET}`;
export const JWT_EXPIRATION = `${process.env.JWT_EXPIRATION}`;
